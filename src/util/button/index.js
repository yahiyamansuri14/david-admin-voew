import React from 'react'

export default function Button(props) {
    const { height, widht, backgroundColor, color } = props
  return (
    <button
        height={height}
        widht={width}
    ></button>
  )
}
